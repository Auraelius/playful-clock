const EXPRESS_PORT = process.env.PORT || 8000;
const NODE_ENV = process.env.NODE_ENV || 'development';
const API_TOKEN = process.env.API_TOKEN || 'dummy-api-token';

export {
  EXPRESS_PORT,
  NODE_ENV,
  API_TOKEN
};
